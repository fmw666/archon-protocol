node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
docs/public/source-files/
docs/public/manifest.json
.DS_Store
*.log

# Local secrets (CURSOR_API_KEY etc.) — see .env.example for the schema.
.env
.env.local
.env.*.local

# EvoMap GEP assets
memory/
assets/

# Archon runtime ledgers (each adopter project owns its own copy; the
# archon-protocol source repository must not commit per-run state).
.archon/runs/
.archon/run.md
