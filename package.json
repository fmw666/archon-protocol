{
  "name": "archon-protocol",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "lint": "node scripts/lint-links.mjs && node scripts/lint-integrity.mjs && vitest run --config vitest.config.js",
    "lint:links": "node scripts/lint-links.mjs",
    "lint:integrity": "node scripts/lint-integrity.mjs",
    "test": "vitest run --config vitest.config.js"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vitest": "^4.0.18"
  }
}
